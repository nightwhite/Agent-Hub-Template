# ubuntu base image

Ubuntu 24.04 based reusable runtime layer for agent images.

Included:
- shell utilities
- network/debugging tools
- Python 3 runtime
- tini as init process
- non-root `agent` user
